# file for plotting out code - don't copy over

# Increment skill rewrite 
#callable multi-purpose function 
#use arrays to store the skill string and increment value 
#store each skillincrement array in a commit skill changes array and cycle through it on commit